__pycache__/
*.pyc
*.egg-info/
.venv/
venv/
.pytest_cache/
build/
dist/
*.spec

# Written by the build to embed the release version (see updater.py)
src/irtracker/_buildinfo.py

# Editors / local tooling
.idea/
.vscode/
.claude/

# Personal iRacing config copies — these belong in Documents\iRacing, not the
# repo. (The intentional test fixtures under tests/corpus/ are NOT affected,
# since these patterns are anchored to the repo root.)
/app.ini
/camera.ini
/controls.cfg
/core.ini
/fueldata.ini
/joyCalib.yaml
/rendererDX11*.ini
